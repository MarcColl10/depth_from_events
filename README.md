# depth_from_events
Efficient self-supervised learning of depth from only events.

## To do

`cmax.py`:
- [ ] Allow base smaller than accumulation window

`uzh_fpv.py`:
- [x] Don't store calibration file, then keep all files in same directory making naming easier
