_target_: network.WrappedDisparityPoseNetwork
encoder_channels: 64
memory_channels: 96
decoder_channels: 96
activation_fn:
  _target_: torch.nn.ReLU
  _partial_: true 
final_bias: true
scaling: [1, 0.01]  # disparity, pose
