cmax:
  _target_: cmax.IterativeContrastMaximization
  use_events: ${....datamodule.return_events}
  accumulation_window: 10
  base: 5
  keep_warping: true  # also check for going out outside base
  bp_frac: -1.0  # <0 = backprop all events
  select: null  # start:stop, null for everything
